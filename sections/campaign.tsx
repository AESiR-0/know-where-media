export default function Campaign() {
    return (
        <div className="min-h-screen flex items-center justify-center bg-campaign">
            <h1 className="campaign-typography ">campaigns that capitave</h1>
        </div>

    );
}